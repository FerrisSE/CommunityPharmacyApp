import React from 'react';
import { Button, Text, View } from 'react-native';

const ServiceScheduling = ({ navigation }) => {
	return (
		<Text>Scheduling screen here</Text>
	);
};

export default ServiceScheduling;